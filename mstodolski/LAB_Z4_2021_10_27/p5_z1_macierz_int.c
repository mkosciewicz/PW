#include <stdio.h>
#define LICZBA_KOL 6
#define LICZBA_WIERSZY 

int main(void)
{
    int tab[LICZBA_WIERSZY][LICZBA_KOL];
    /*  tab[0][0] - pierwsza wartosc w macierzy
    **  &tab[0][0] - adres pierszego elementu
    */
    int *t = &tab[0][0];

    for (int w = 0; w < LICZBA_WIERSZY; ++w)
    {   for 
    }
}